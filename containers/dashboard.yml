version: "3"

services:
  heimdall:
    iamge: lscr.io/linuxserver/heimdall
    container_name: dashboard
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - ./config:/config
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped

